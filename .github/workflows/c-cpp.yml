name: CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Debug Test
    steps:

    - name: Set up GCC
      uses: egor-tensin/setup-gcc@v1
      with:
        version: 12
        platform: x64

    - name: Check out repository
      uses: actions/checkout@v3

    - name: Install dependencies
      run: sudo apt install -y libaio-dev gcovr lcov

    - name: Config project
      run: cmake -B build/debug -S . -DCMAKE_BUILD_TYPE=Debug -DBUILD_WITH_COVERAGE=ON

    - name: Build project
      run: cmake --build build/debug -j `nproc`

    - name: Test
      run: ctest --test-dir build/debug

    - name: Generate coverage file
      run: gcovr -r . --json-pretty --json=coverage.json --xml-pretty --xml=coverage.xml --exclude 'build/*' --exclude 'tests/*'

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v3
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      with:
        files: coverage.xml
        verbose: true

    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
      with:
        limit-access-to-actor: true
